%! Package = cs581
%! Author = Len Washington III
%! Date = 12/26/23

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{cs581}[2023/12/26 A styling package for the class: ]

\RequirePackage{kvoptions}

\SetupKeyvalOptions{
	family=cs,
	prefix=cs@
}

\DeclareStringOption{title}
\DeclareStringOption[Len Washington III]{author}
\DeclareStringOption[{\today}]{date}
\DeclareStringOption[Advanced Artificial Intelligence]{subject}
\DeclareStringOption[CS 581]{subjectCode}

\ProcessKeyvalOptions*

\RequirePackage[author=\cs@author,
	title={\cs@title},
	date={\cs@date},
	subject=\cs@subject,
	code=\cs@subjectCode
]{iitacademics}
\RequirePackage{tikz-qtree}

\definecolor{Peas}{HTML}{ff0000}
\definecolor{pEas}{HTML}{00b050}
\definecolor{peAs}{HTML}{0070c0}
\definecolor{peaS}{HTML}{7030a0}

\newcommand{\Peas}{\textcolor{Peas}{P}}
\newcommand{\pEas}{\textcolor{pEas}{E}}
\newcommand{\peAs}{\textcolor{peAs}{A}}
\newcommand{\peaS}{\textcolor{peaS}{S}}

\newcolumntype{P}[1]{>{\centering\arraybackslash}p{#1}}
\newcommand{\definition}[2]{\textbf{\textcolor{red}{#1}} -- #2}
\let\oldemph\emph
\renewcommand{\emph}[1]{\textcolor{red}{#1}}

\usetikzlibrary{scopes,trees,positioning,graphs,arrows.meta,shapes,shapes.geometric,decorations.markings}
\newcommand{\newtikzset}{
	\tikzset{every node/.style={minimum width=2em,draw,circle},
		blank/.style={draw=none},
		edge from parent/.style=
			{draw,edge from parent path={(\tikzparentnode) -- (\tikzchildnode)}},
		level distance=1cm,
		sibling distance=5mm}
}

\newcommand{\nodeset}[1][.]{
	\ifinanswer%
	\colorlet{current}{blue}%
	\else%
	\colorlet{current}{#1}%
	\fi%
	\tikzset{every node/.style={current,circle,draw}}
}

\newcommand{\pathset}[1][.]{
	\ifinanswer%
	\colorlet{current}{blue}%
	\else%
	\colorlet{current}{#1}%
	\fi%
	\tikzset{>={Stealth[current]},
		every node/.style={current,fill=white,circle},
		every edge/.style={current,draw,very thick}}%draw=current instead of current,draw?
}